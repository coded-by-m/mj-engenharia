# Execution Plan

> Generated by Coded by M — Site Architecture System. Source of truth: `/docs/ai/DISCOVERY.md` and `/docs/ai/DECISIONS_LOG.md`.

## Required Reading Order
1. `/CLAUDE.md`
2. `/docs/ai/MASTER_CONTEXT.md`
3. `/docs/ai/PROJECT.md`
4. `/docs/ai/SITE_ARCHITECTURE.md`
5. `/docs/ai/PAGE_BLUEPRINTS.md`
6. `/docs/ai/DESIGN_SYSTEM.md`
7. `/docs/ai/MOTION_DIRECTION.md`
8. `/docs/ai/CONTENT_PENDING.md`
9. `/docs/ai/QA_CHECKLIST.md`

## Stack
Next.js (App Router) + Tailwind CSS. `next/font` (Montserrat, Inter, Barlow Condensed). `next/image`. Deploy on Vercel. WhatsApp via `wa.me` deep links.

## Phase 01 — Project foundation
### Goal
Scaffold Next.js + Tailwind app; install fonts; define design tokens as CSS variables / Tailwind theme from `DESIGN_SYSTEM.md`; set global layout, container, metadata, favicons (from `Identidade Visual/Favicon`); copy brand assets into `/public`.
### Execution Prompt
Read all required docs. Diagnose before editing. Implement only this phase. Report changed files, decisions and pending issues.
### Deliverables
- App scaffold, Tailwind config with brand tokens, font setup, base reset, global container, metadata + Schema.org LocalBusiness stub, assets in `/public`.

## Phase 02 — Essential components
Build reusable: Header (fixed, condensing), Footer, Button (primary/secondary/ghost), WhatsApp CTA + deep-link helper, Floating WhatsApp button, Section header (eyebrow/H2/sub), Card, Form fields, Tag/badge, Icon wrapper for brand SVGs.

## Phase 03 — Page structure
Assemble all sections in order from `SITE_ARCHITECTURE.md` / `PAGE_BLUEPRINTS.md` with semantic HTML and anchors. Wire smooth-scroll nav, scroll-spy, and all WhatsApp CTAs. Use placeholder content per `CONTENT_PENDING.md` (clearly marked).

## Phase 04 — Visual direction pass
Apply DESIGN_SYSTEM precisely: spacing, grid alignment, typography scale, color discipline (red only on CTAs), card styling, navy inverse sections, imagery overlays. Make it visibly more organized/premium than the reference.

## Phase 05 — Motion and interactions
Implement MOTION_DIRECTION: hero sequence, on-scroll reveals (IntersectionObserver), staggered cards, hover elevations, count-up metrics, CTA microinteraction, floating button entrance. Honor `prefers-reduced-motion`.

## Phase 06 — Responsive pass
Desktop / notebook / tablet / mobile. Header collapse, full-width CTAs, single-column stacks, ≥44px touch targets, no overflow.

## Phase 07 — Quality pass
Run `QA_CHECKLIST.md`: structure, responsive, accessibility, SEO, performance (Core Web Vitals), console clean. Confirm no unresolved visible placeholders before deploy (or list remaining pending content).

## Completion Criteria
Use `/docs/ai/QA_CHECKLIST.md` as the project readiness gate. Deploy to Vercel once pending content (WhatsApp number, CREA, address, real images) is supplied or explicitly accepted as placeholder.
